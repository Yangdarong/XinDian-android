<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/ll_wait_confirm"
    tools:context=".fragment.infoDetailPage.WaitConfirmPageFragment">

    <TextView
        android:id="@+id/tv_wait_non"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="当前没有需要处理的订单~"
        android:drawableTop="@drawable/pic_wait_nothing"
        android:textSize="25sp"
        android:gravity="center_horizontal"/>

    <!-- TODO: Update blank fragment layout -->
    <ListView
        android:id="@+id/lv_wait_confirm"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</LinearLayout>